# Local (API on host, Postgres in Docker): use localhost
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/llm_teacher

# When the API runs in Docker Compose, docker-compose.yml sets:
#   DATABASE_URL=postgresql://postgres:postgres@db:5432/llm_teacher
#   OLLAMA_HOST=http://host.docker.internal:11434
# You do not need those in this file for `docker compose up`.
